[workspace]
resolver = "2"
members = [".", "xtask", "trueno-gpu", "trueno-explain", "crates/cbtop", "crates/trueno-quant", "crates/trueno-sparse", "crates/trueno-fft", "crates/trueno-solve", "crates/trueno-rand", "crates/trueno-image", "crates/trueno-tensor", "trueno-cuda-edge", "trueno-ptx-debug"]
exclude = []

# Workspace-level lints (PMAT best practices)
[workspace.package]
edition = "2021"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "warn"
unreachable_pub = "allow"
missing_docs = "allow"
rust_2018_idioms = { level = "allow", priority = -1 }
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(internal_matrix_tests)'] }

[workspace.lints.clippy]
# Correctness (critical)
correctness = { level = "deny", priority = -1 }
suspicious = { level = "warn", priority = -1 }

# Performance
perf = { level = "warn", priority = -1 }

# Known Defects (v2.200.0 - Cloudflare outage 2025-11-18)
# NOTE: These are allowed at workspace level to unblock -D warnings CI.
# Enforcement is via targeted audits and pmat tooling instead.
unwrap_used = "allow"
expect_used = "allow"
panic = "allow"
unreachable = "allow"

# Code quality
complexity = { level = "warn", priority = -1 }
style = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }

# Additional high-value lints (PMAT best practices)
fallible_impl_from = "warn"

# Development-phase allows (to be addressed incrementally)
# These pedantic/style lints are allowed at workspace level to enable
# -D warnings CI. They should be addressed in focused cleanup sprints.
uninlined_format_args = "allow"
useless_vec = "allow"
unnecessary_unwrap = "allow"
doc_markdown = "allow"
must_use_candidate = "allow"
return_self_not_must_use = "allow"
cast_lossless = "allow"
wildcard_imports = "allow"
semicolon_if_nothing_returned = "allow"
items_after_statements = "allow"
needless_raw_string_hashes = "allow"
format_push_string = "allow"
similar_names = "allow"
redundant_closure = "allow"
redundant_closure_for_method_calls = "allow"
cast_possible_wrap = "allow"
cast_sign_loss = "allow"
cast_precision_loss = "allow"
cast_possible_truncation = "allow"
cast_ptr_alignment = "allow"
checked_conversions = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
module_name_repetitions = "allow"
too_many_lines = "allow"
too_many_arguments = "allow"
unused_async = "allow"
trivially_copy_pass_by_ref = "allow"
match_same_arms = "allow"
ptr_as_ptr = "allow"
used_underscore_binding = "allow"
used_underscore_items = "allow"
unused_self = "allow"
needless_pass_by_value = "allow"
single_match_else = "allow"
unreadable_literal = "allow"
excessive_precision = "allow"
inline_always = "allow"
option_if_let_else = "allow"
map_unwrap_or = "allow"
or_fun_call = "allow"
default_trait_access = "allow"
many_single_char_names = "allow"
struct_excessive_bools = "allow"
manual_let_else = "allow"
implicit_clone = "allow"
unnecessary_wraps = "allow"
iter_without_into_iter = "allow"
manual_assert = "allow"
if_not_else = "allow"
linkedlist = "allow"
match_wildcard_for_single_variants = "allow"
bool_to_int_with_if = "allow"
clone_on_ref_ptr = "allow"
explicit_iter_loop = "allow"
needless_lifetimes = "allow"
elidable_lifetime_names = "allow"
map_entry = "allow"
assigning_clones = "allow"
result_unit_err = "allow"
missing_fields_in_debug = { level = "allow", priority = 1 }
cloned_instead_of_copied = "allow"
borrow_as_ptr = "allow"
needless_question_mark = "allow"

[package]
name = "trueno"
version = "0.16.3"
edition = "2021"
rust-version = "1.89"
authors = ["Pragmatic AI Labs"]
license = "MIT"
description = "High-performance SIMD compute library with GPU support for matrix operations"
repository = "https://github.com/paiml/trueno"
readme = "README.md"
keywords = ["simd", "gpu", "wasm", "performance", "vectorization"]
categories = ["algorithms", "mathematics", "science"]
exclude = ["target/", ".profraw", ".profdata", ".vscode/", ".idea/", ".pmat", "proptest-regressions"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--generate-link-to-definition"]

[lints]
workspace = true

[dependencies]
thiserror = "2.0.17"
serde = { version = "1.0", features = ["derive"] }  # Hardware capability serialization (PMAT-447)
serde_json = "1.0"  # ML tuner data serialization (TUNER-010)
rayon = { version = "1.11", optional = true }
wgpu = { version = "27.0", optional = true }
pollster = { version = "0.4", optional = true }
bytemuck = { version = "1.24", features = ["derive"], optional = true }
futures-intrusive = { version = "0.5", optional = true }
# WASM async support for GPU
wasm-bindgen-futures = { version = "0.4", optional = true }
wasm-bindgen = { version = "0.2", optional = true }
# Native CUDA monitoring via trueno-gpu (TRUENO-SPEC-010)
trueno-gpu = { version = "0.4.0", path = "trueno-gpu", optional = true }
# PMAT-336: Provable contracts compile-time enforcement
provable-contracts-macros = { path = "../provable-contracts/crates/provable-contracts-macros" }
# Tracing/profiling support (renacer integration)
tracing = { version = "0.1", optional = true }
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"], optional = true }
tracing-appender = { version = "0.2", optional = true }
dirs = { version = "5.0", optional = true }  # Hardware capability path detection (PMAT-447)
# TUI monitoring (TRUENO-SPEC-020)
ratatui = { version = "0.29", optional = true }
crossterm = { version = "0.28", optional = true }
# Stress test reporting types compatible with renacer (TRUENO-SPEC-025)
# Note: renacer 0.9.1 has compilation issue; using compatible local types instead
dhat = { version = "0.3", optional = true }
trueno-quant = { version = "0.1", path = "crates/trueno-quant" }
num_cpus = "1.17.0"
anyhow = "1.0.100"
# Hardware capability detection (PMAT-447)
chrono = "0.4"
toml = "0.8"
# ML tuner integration (SHOWCASE-BRICK-001, Section 12)
# Uses aprender 0.24.0 RandomForest for learned optimization
aprender = { version = "0.26", optional = true, default-features = false }
# Execution path graph (PAR-201, Section E.7)
trueno-graph = { version = "0.1.12", optional = true }
# TUI visualization for execution graphs (PAR-201)
presentar-terminal = { version = "0.3", optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
# Web-sys for WebGPU (auto-enabled when gpu-wasm feature is on)
web-sys = { version = "0.3", optional = true, features = ["console"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
# Hostname detection only on native (not available on WASM)
hostname = "0.4"

[build-dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml_ng = "0.10"

[dev-dependencies]
proptest = "1.9"
criterion = "0.7"
tokio = { version = "1", features = ["macros", "rt"] }
serde_json = "1.0"
nalgebra = "0.34"  # For eigendecomposition benchmark comparison
# Simulation testing framework (TRUENO-SPEC-012)
simular = "0.2.0"
regex = "1.11"  # For PTX/WGSL pattern validation in falsification tests
# GPU edge-case test framework (TCE-001)
trueno-cuda-edge = { version = "0.1", path = "trueno-cuda-edge" }
# Sparse and solver crates for contract tests
trueno-sparse = { version = "0.1", path = "crates/trueno-sparse" }
trueno-solve = { version = "0.1", path = "crates/trueno-solve" }
# Contract trait enforcement (Section 23)
provable-contracts = { path = "../provable-contracts/crates/provable-contracts" }

[features]
default = []
parallel = ["rayon"]
gpu = ["wgpu", "pollster", "bytemuck", "futures-intrusive"]
# GPU for WASM (WebGPU) - uses wasm-bindgen-futures instead of pollster
gpu-wasm = ["wgpu", "bytemuck", "futures-intrusive", "wasm-bindgen-futures", "wasm-bindgen", "web-sys"]
# Native CUDA monitoring via trueno-gpu (TRUENO-SPEC-010)
# Provides accurate device info (e.g., "NVIDIA GeForce RTX 4090") and real-time memory metrics
cuda-monitor = ["trueno-gpu/cuda"]
# Chaos engineering features (from renacer v0.4.1)
chaos-basic = []
chaos-network = ["chaos-basic"]
chaos-byzantine = ["chaos-basic"]
chaos-full = ["chaos-network", "chaos-byzantine"]
# Tracing/profiling (renacer integration)
tracing = ["dep:tracing"]
# Hardware capability detection with home directory support (PMAT-447)
hardware-detect = ["dep:dirs"]
# TUI monitoring (TRUENO-SPEC-020) with stress reporting and logging
tui-monitor = ["ratatui", "crossterm", "trueno-gpu", "dep:tracing", "tracing-subscriber", "tracing-appender", "dep:dirs"]
# CUDA support for TUI (real hardware monitoring)
cuda = ["trueno-gpu/cuda"]
# ML-based kernel selection and throughput prediction (SHOWCASE-BRICK-001)
# Uses aprender RandomForest for learned optimization
ml-tuner = ["aprender"]
# Execution path graph for profiling (PAR-201)
# Tracks PTX→kernel→brick call relationships, exports to CsrGraph
execution-graph = ["trueno-graph"]
# TUI visualization for execution graphs (PAR-201)
# Uses presentar-terminal Tree widget for hierarchical display
presentar-tui = ["presentar-terminal"]
# Heap profiling via dhat-rs (DHAT viewer: https://nnethercote.github.io/dh_view/dh_view.html)
dhat-heap = ["dep:dhat"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = true  # Enable debug symbols for profiling (flamegraph, perf)

[lib]
name = "trueno"
path = "src/lib.rs"

[[bench]]
name = "vector_ops"
harness = false

[[bench]]
name = "matrix_ops"
harness = false

[[bench]]
name = "gpu_ops"
path = "benches/gpu_ops/main.rs"
harness = false
required-features = ["gpu"]

[[bench]]
name = "gpu_reduction"
harness = false
required-features = ["gpu"]

[[bench]]
name = "async_gpu_ops"
harness = false
required-features = ["gpu"]

[[bench]]
name = "eigen_ops"
harness = false

[[bench]]
name = "tiling_ops"
harness = false

[[bin]]
name = "trueno-monitor"
path = "src/bin/trueno_monitor/main.rs"
required-features = ["tui-monitor"]

[[example]]
name = "gpu_batch_demo"
required-features = ["gpu"]

[[example]]
name = "gpu_tiled_reduction"
required-features = ["gpu"]

[package.metadata.release]
shared-version = true

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = "## \\[Unreleased\\]"
replace = "## [{{version}}] - {{date}}"

[workspace.dependencies]

[profile.test]
opt-level = 1              # Slight optimization for faster test execution
incremental = true         # Reuse previous builds

[profile.dev]
panic = "abort"
