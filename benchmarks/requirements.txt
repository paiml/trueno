# Python dependencies for Trueno benchmark comparison suite
#
# Install with: pip install -r benchmarks/requirements.txt

# NumPy - Python's standard numerical computing library
numpy>=1.24.0

# PyTorch - Deep learning framework with optimized CPU operations
torch>=2.0.0

# For JSON processing (included in Python stdlib, listed for reference)
# json (built-in)

# For timing (included in Python stdlib, listed for reference)
# time (built-in)
# statistics (built-in)
